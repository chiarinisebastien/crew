<hr>
<form method="post" action="{{ path('identifiant_delete', {'id': identifiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ identifiant.id) }}">
	<button class="btn btn-danger btn-sm col-1">Delete</button>
</form>
