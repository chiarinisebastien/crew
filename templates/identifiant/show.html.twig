{% extends 'base.html.twig' %}

{% block title %}Identifiant{% endblock %}

{% block body %}
    <h1>Identifiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ identifiant.id }}</td>
            </tr>
            <tr>
                <th>Quoi</th>
                <td>{{ identifiant.quoi }}</td>
            </tr>
            <tr>
                <th>Ou</th>
                <td>{{ identifiant.ou }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ identifiant.identifiant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('identifiant_index') }}">back to list</a>

    <a href="{{ path('identifiant_edit', {'id': identifiant.id}) }}">edit</a>

    {{ include('identifiant/_delete_form.html.twig') }}
{% endblock %}
