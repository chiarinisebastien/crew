{% extends 'base.html.twig' %}

{% block title %}ListeTache index{% endblock %}

{% block body %}
    <h1>ListeTache index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for liste_tache in liste_taches %}
            <tr>
                <td>{{ liste_tache.id }}</td>
                <td>
                    <a href="{{ path('liste_tache_show', {'id': liste_tache.id}) }}">show</a>
                    <a href="{{ path('liste_tache_edit', {'id': liste_tache.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('liste_tache_new') }}">Create new</a>
{% endblock %}
