{{ form_start(formTache) }}

<div class="row">

    <div class="col-6">{{ form_row(formTache.service) }}</div>

</div>



<div class="row">

    <div class="col-12">{{ form_row(formTache.submit) }}</div>

</div>

{{ form_end(formTache) }}
