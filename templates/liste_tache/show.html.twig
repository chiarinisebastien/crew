{% extends 'base.html.twig' %}

{% block title %}ListeTache{% endblock %}

{% block body %}
    <h1>ListeTache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_tache.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('liste_tache_index') }}">back to list</a>

    <a href="{{ path('liste_tache_edit', {'id': liste_tache.id}) }}">edit</a>

    {{ include('liste_tache/_delete_form.html.twig') }}
{% endblock %}
