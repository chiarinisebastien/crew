<form method="post" action="{{ path('liste_tache_delete', {'id': liste_tache.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_tache.id) }}">
    <button class="btn">Delete</button>
</form>
