<form method="post" action="{{ path('admin_departure_delete', {'id': admin_departure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_departure.id) }}">
    <button class="btn">Delete</button>
</form>
