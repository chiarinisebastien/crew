{% extends 'base.html.twig' %}

{% block title %}Pointage{% endblock %}

{% block body %}
    <h1>Pointage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pointage.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ pointage.createdAt ? pointage.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Onoff</th>
                <td>{{ pointage.onoff }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pointage_index') }}">back to list</a>

    <a href="{{ path('pointage_edit', {'id': pointage.id}) }}">edit</a>

    {{ include('pointage/_delete_form.html.twig') }}
{% endblock %}
