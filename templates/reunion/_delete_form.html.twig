<hr>
<form method="post" action="{{ path('reunion_delete', {'id': reunion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reunion.id) }}">
    <button class="btn btn-sm col-1 btn-danger">Delete</button>
</form>
