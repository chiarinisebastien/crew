{{ form_start(form) }}

<div class="row">

    <div class="col-3">{{ form_row(form.username) }}</div>
    <div class="col-3">{{ form_row(form.firstname) }}</div>
    <div class="col-3">{{ form_row(form.lastname) }}</div>

</div>

<div class="row">
    <div class="col-3">
        <div class="col-12">{{ form_row(form.roles) }}</div>
    </div>
    <div class="col-3">{{ form_row(form.departure) }}</div>

</div>


<div class="row">

    <div class="col-12">{{ form_row(form.submit) }}</div>

</div>

{{ form_end(form) }}
