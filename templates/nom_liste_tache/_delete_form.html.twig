<hr>
<form method="post" action="{{ path('nom_liste_tache_delete', {'id': nom_liste_tache.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nom_liste_tache.id) }}">
    <button class="btn btn-danger col-1 btn-sm">Delete</button>
</form>
