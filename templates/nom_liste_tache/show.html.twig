{% extends 'base.html.twig' %}

{% block title %}NomListeTache{% endblock %}

{% block body %}
    <h1>NomListeTache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_liste_tache.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ nom_liste_tache.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nom_liste_tache_index') }}">back to list</a>

    <a href="{{ path('nom_liste_tache_edit', {'id': nom_liste_tache.id}) }}">edit</a>

    {{ include('nom_liste_tache/_delete_form.html.twig') }}
{% endblock %}
