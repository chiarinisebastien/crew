{% extends 'base.html.twig' %}

{% block title %}Yesorno{% endblock %}

{% block body %}
    <h1>Yesorno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ yesorno.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ yesorno.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('yesorno_index') }}">back to list</a>

    <a href="{{ path('yesorno_edit', {'id': yesorno.id}) }}">edit</a>

    {{ include('yesorno/_delete_form.html.twig') }}
{% endblock %}
