<hr>
<form method="post" action="{{ path('yesorno_delete', {'id': yesorno.id}) }}"
    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yesorno.id) }}">
    <button class="btn btn-danger btn-sm col-1">Delete</button>
</form>